version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - echo install
  pre_build:
    commands:
      - echo pre build
  build:
    commands:
      - echo pre build
  post_build:
    commands:
      - echo pre build
  
artifacts:
  files:
    - '*/*'
